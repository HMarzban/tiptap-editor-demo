/**
 * Editor surface: must match the `.tiptap` rules in `src/index.css`.
 */
export const EDITOR_ROOT_CLASS_NAME = "tiptap";

export const DEFAULT_INITIAL_HTML = "<p>Start writing...</p>";

export const DEFAULT_PLACEHOLDER_TEXT = "Start typing here...";

/** Used for “min read” estimates in the status bar. */
export const READING_SPEED_WORDS_PER_MINUTE = 200;
